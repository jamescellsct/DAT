/**
 *
 * DndGrid
 *
 */
import React, { memo } from 'react';
import styled from 'styled-components/macro';

interface Props {}

export const DndGrid = memo((props: Props) => {
  return <Div></Div>;
});

const Div = styled.div``;
